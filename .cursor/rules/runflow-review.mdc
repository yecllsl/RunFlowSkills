---
description: runflow-review Skill
globs: "**/*"
alwaysApply: false
---

---
name: runflow-review
description: Use when 用户想复盘训练、生成总结、回顾本周/本月训练、导出报告
version: 1.0.0
allowed-tools: [get_period_summary, export_data, save_decision_log]
inputs:
  - name: period
    type: enum
    values: [week, month, season, year]
    required: false
    default: week
    description: 复盘周期
  - name: date_ref
    type: string
    required: false
    description: 参考日期 YYYY-MM-DD，默认今日
  - name: compare
    type: enum
    values: [none, previous, yoy]
    required: false
    default: previous
    description: 对比方式（环比/同比）
outputs:
  - name: summary
    type: object
    description: 总距离/总 TSS/平均 VDOT/训练次数
  - name: trends
    type: object
    description: VDOT 趋势 / HRV 趋势
  - name: load_change
    type: number
    description: vs 上周期负荷变化
  - name: prompt
    type: string
    description: 传递给宿主 LLM 的复盘报告提示词
error_handling:
  - insufficient_data: "标注 '基于 N 天数据，置信度低'"
  - export_failed: "提示重试并提供原始数据"
  - ai_anomaly: "提示重试 + 提供原始数据"
compatibility:
  mcp_server: ">=0.1.1"
  python: ">=3.12"
  platforms: [trae, claude-code, cursor, windsurf, continue]
---

# 训练复盘流程

## Overview

训练复盘助手，负责聚合周期数据、对比历史、生成报告。核心流程：确定周期 → 聚合数据 → 对比 → AI 报告 → 可选导出。

## When to Use

- 用户说"复盘"/"总结"/"本周"/"本月"/"回顾"
- 用户想了解某段时间的训练表现
- 用户需要导出训练报告

## Workflow

### 1. 确定周期

默认本周，支持 week/month/season/year。

### 2. 聚合周期数据

调用 `get_period_summary(period, date_ref)`：
- 返回 total_distance / total_tss / avg_vdot / sessions_count
- 返回 vdot_trend / hrv_trend
- 返回 load_change（vs 上周期变化）

### 3. 对比历史

- 环比：vs 上周期（get_period_summary 已含 load_change）
- 同比：vs 去年同期（需调用两次 get_period_summary）

### 4. AI 生成报告

用 Tool 返回的 `prompt` 调用宿主 LLM 生成复盘报告：
- 跑量统计（总距离/训练次数）
- 负荷变化（CTL/ATL/TSB 趋势）
- VDOT 趋势（进步/停滞/退步）
- HRV 趋势（恢复状态）
- 伤病风险评估
- 下周建议

### 5. 可选导出

用户确认后调用 `export_data(format="md")` 导出 Markdown 报告。
**导出前必须用户确认**（data-safety-rules.md）。

### 6. 持久化决策

调用 `save_decision_log` 记录复盘结论。

## Quick Reference

| 步骤 | Tool | 说明 |
|------|------|------|
| 周期聚合 | `get_period_summary` | week/month/season/year |
| 导出报告 | `export_data` | md/csv/json/parquet |
| 决策记录 | `save_decision_log` | 记录复盘结论 |

## Common Mistakes

- **复盘笼统**：必须具体到数据（"本周跑量 45km，较上周 +5km"）
- **缺数据维度静默跳过**：必须明确标注 "HRV 数据缺失，未纳入分析"
- **导出前未确认**：export_data 前必须用户确认
- **同比/环比不明确**：必须标注时间窗口

## 约束规则

> 规则统一收敛至根目录 [AGENTS.md](../../../AGENTS.md)，本节仅列与本 Skill 直接相关的条目。

- 遵循 `AGENTS.md §4 分析规则`：复盘必须具体、附依据
- 遵循 `AGENTS.md §6 数据安全规则`：导出前用户确认，含 AI 日志时二次确认
- 遵循 `AGENTS.md §2 交互协议`：错误时提供降级方案
